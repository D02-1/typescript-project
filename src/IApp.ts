// Professionelle klassenbasierende projekte in programiersprachen wie C# beinhalten sogenannte interfaces, dies sind kleine objekte, die sagen welche dateitypen und informationen die zu übergebenen parameter beinhalten müssen.

/**
 * @interface IApp
 */
export default interface IApp
{
    counter : number, root : string
}
